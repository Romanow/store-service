services:
  warranty-url: http://localhost:${wiremock.server.port}
  warehouse-url: http://localhost:${wiremock.server.port}

oauth2:
  security:
    enabled: false

management:
  tracing:
    enabled: false
