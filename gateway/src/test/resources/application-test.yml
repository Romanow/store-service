spring:
  main:
    allow-bean-definition-overriding: true
  testcontainers:
    dynamic-property-registry-injection: allow
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: store
            client-secret: kIrcBrIz9OvgnDKQEUAi8XsiI6ekzUZC

services:
  test:
    url: http://localhost:8180
    pattern: /test/**
    path: openapi/test.yml

oauth2:
  security:
    enabled: true

management:
  tracing:
    enabled: false

logging:
  level:
    org.springframework.security: debug
